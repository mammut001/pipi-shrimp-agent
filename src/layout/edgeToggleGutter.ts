/**
 * Horizontal room reserved so page chrome is not covered by MainLayout's
 * right-panel chevron (`absolute right-3 top-4 h-8 w-8`). Wider than `mr-12`
 * so labels such as 「保存」 stay fully visible at 1280px.
 */
export const MAIN_LAYOUT_EDGE_TOGGLE_GUTTER_CLASS = 'pr-20';
