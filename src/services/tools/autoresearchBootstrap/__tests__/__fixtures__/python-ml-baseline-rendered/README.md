# test-project

## Goal
Improve test accuracy

## Success Criteria
Beat the baseline by at least 1 point.

## Primary Metric
accuracy

## Baseline
ResNet50 on CIFAR10

## Entry Command
Run `python3 train.py` and `python3 eval.py` through `python3 run_experiment.py`.